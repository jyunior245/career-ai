---

description: "Task list for CareerAI feature implementation — generated by /speckit.tasks"
---

# Tasks: CareerAI — Resume Analysis Platform

**Input**: Design documents from `.specify/templates/`

**Prerequisites**: `plan-template.md` ✅, `spec-template.md` ✅, `constitution-template.md` ✅

**Tests**: Included — pytest with mocked external dependencies (Groq API, pdfplumber).

**Organization**: Tasks grouped by user story to enable independent implementation and validation of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1, US2, US3)

## Path Conventions

- **Backend**: `backend/app/`, `backend/tests/`
- **Frontend**: `frontend/templates/`, `frontend/static/`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project structure, virtual environment, and base configuration.

- [x] T001 Create project directory structure (`backend/`, `frontend/`, `docs/`)
- [x] T002 Initialize Python virtual environment (`python -m venv venv`)
- [x] T003 [P] Create `backend/requirements.txt` with FastAPI, uvicorn, pdfplumber, groq, pydantic, pytest, httpx, python-multipart
- [x] T004 [P] Create `requirements-frontend.txt` with Flask, httpx, python-dotenv
- [x] T005 [P] Create `.gitignore` (venv/, .env, __pycache__, *.db, uploads/)
- [x] T006 [P] Create `backend/.env.example` with `GROQ_API_KEY`, `DATABASE_URL`, `DEBUG`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before any user story can be implemented.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [x] T007 Implement `backend/app/config.py` — loads `GROQ_API_KEY`, `GROQ_MODEL`, `DATABASE_URL`, `DEBUG` from environment via python-dotenv; exposes `settings` singleton
- [x] T008 [P] Implement `backend/app/data/database.py` — SQLite connection, `create_tables()`, `save_analysis()` using stdlib `sqlite3`
- [x] T009 [P] Implement `backend/app/presentation/schemas.py` — `AnalysisResponse(BaseModel)` with `compatibility_score: int`, `strengths: List[str]`, `weaknesses: List[str]`, `suggestions: List[str]`
- [x] T010 Implement `backend/app/main.py` — FastAPI app factory, includes router, calls `db.create_tables()` on startup, configures logging
- [x] T011 Configure logging (`logging.basicConfig`) at INFO level in both `app.py` (Flask) and `backend/app/main.py` (FastAPI)

**Checkpoint**: Foundation ready — all user story tasks can now begin.

---

## Phase 3: User Story 1 — Upload de Currículo e Receber Análise (Priority: P1) 🎯 MVP

**Goal**: Full end-to-end flow: PDF upload → text extraction → Groq AI analysis → results page.

**Independent Test**: Upload a real PDF resume with a job description ≥ 50 chars → results page shows score, strengths, weaknesses, suggestions.

### Tests for User Story 1 ⚠️

> Tests written FIRST, verified to FAIL before implementation.

- [x] T012 [P] [US1] Unit tests for `PDFService.extract_text_from_pdf()` in `backend/tests/test_pdf_service.py` — test valid PDF, empty PDF, corrupted PDF, oversized PDF
- [x] T013 [P] [US1] Unit tests for `AnalysisService._parse_response()` and `_build_prompt()` in `backend/tests/test_analysis_service.py` — test valid JSON, score clamping, invalid JSON exception
- [x] T014 [P] [US1] `backend/tests/conftest.py` — shared fixtures: sample resume text, sample job description, mock Groq response

### Implementation for User Story 1

- [x] T015 [P] [US1] Implement `backend/app/service/pdf_service.py` — `PDFService.extract_text_from_pdf(file_path: str) -> Tuple[bool, str]` using pdfplumber; validates min/max length
- [x] T016 [P] [US1] Implement `backend/app/service/groq_service.py` — `GroqService.analyze_resume(resume_text: str, job_description: str) -> Dict[str, Any]`; uses `response_format={"type": "json_object"}`; `_build_prompt()` and `_parse_response()` private methods
- [x] T017 [US1] Implement `backend/app/presentation/routes.py` — `POST /api/v1/analyze`: validates job_description → validates PDF filename → saves to uploads/ with uuid4 → extracts text → sanitizes → calls GroqService → saves to DB → deletes file in `finally` → returns `AnalysisResponse`; `GET /api/v1/health`
- [x] T018 [US1] Implement `frontend/templates/index.html` — Bootstrap 5 form with PDF file input, job description textarea, submit button, error message display area
- [x] T019 [US1] Implement `frontend/templates/results.html` — Bootstrap 5 page showing compatibility score (badge/progress bar), strengths list, weaknesses list, suggestions list, "Analyze Another" button
- [x] T020 [US1] Implement `app.py` (Flask) — routes `/` (index), `/analyze` (POST: calls FastAPI via httpx, renders results.html), `/health`; configures Flask with `template_folder='frontend/templates'`

**Checkpoint**: User Story 1 fully functional. Upload PDF + job description → see analysis results. ✅

---

## Phase 4: User Story 2 — Rejeição de Uploads Inválidos (Priority: P2)

**Goal**: Robust validation and clear error messages for all invalid upload scenarios.

**Independent Test**: Send non-PDF file, oversized file, and empty job description — each returns a distinct, user-friendly error message without HTTP 500.

### Tests for User Story 2 ⚠️

- [x] T021 [P] [US2] Unit tests for `InputValidator` in `backend/tests/test_input_validator.py` — validate_job_description (min/max chars), validate_pdf_filename (extension check), sanitize_text (null chars, excessive whitespace)

### Implementation for User Story 2

- [x] T022 [P] [US2] Implement `backend/app/service/input_validator.py` — `InputValidator.validate_job_description()`, `validate_pdf_filename()`, `sanitize_text()` — all static methods with type hints
- [x] T023 [US2] Wire `InputValidator` calls into `backend/app/presentation/routes.py` at the top of the route handler — job_description validation → filename validation → size check (before pdfplumber)
- [x] T024 [US2] Add frontend JS validation in `frontend/static/js/main.js` — client-side checks: file extension `.pdf`, job description length (50–10.000 chars), show error messages before form submit

**Checkpoint**: All invalid inputs (wrong file type, oversized, short job description, empty PDF) return clear 400 errors. ✅

---

## Phase 5: User Story 3 — Persistência da Análise (Priority: P3)

**Goal**: Every successful analysis is automatically saved to SQLite.

**Independent Test**: Complete an analysis → inspect `career_ai.db` → verify row exists in `analyses` table with all fields populated.

### Implementation for User Story 3

- [x] T025 [US3] Implement `analyses` table creation in `database.py` with schema: `id`, `resume_text`, `job_description`, `compatibility_score`, `strengths`, `weaknesses`, `suggestions`, `created_at`
- [x] T026 [US3] Implement `db.save_analysis()` in `database.py` — accepts all analysis fields, wraps in try/except to avoid blocking response if DB write fails
- [x] T027 [US3] Wire `db.save_analysis()` call in `routes.py` after successful GroqService response, before returning `AnalysisResponse`

**Checkpoint**: All three user stories functional and independently testable. ✅

---

## Phase 6: Infrastructure & Deploy

**Purpose**: Containerization and cloud deployment readiness.

- [x] T028 [P] Create `Dockerfile` — python:3.12-slim base, copies `backend/` and `frontend/`, installs both requirements files, exposes ports 5000 and 8000
- [x] T029 [P] Create `docker-compose.yml` — `backend` service (uvicorn, :8000), `frontend` service (Flask app.py, :5000), `career-ai-network` bridge; `GROQ_API_KEY` env var injected
- [x] T030 [P] Create `render.yaml` — Blueprint with `career-ai-backend` (FastAPI) and `career-ai-frontend` (Flask) web services; `GROQ_API_KEY` marked `sync: false`

---

## Phase 7: Polish & Cross-Cutting Concerns

- [x] T031 [P] Update `README.md` — replace Ollama/OpenAI references with Groq API, update prerequisites and env var documentation
- [x] T032 Create `docs/` directory with `constitution.md`, `schema.md`, `plan.md` for GitHub visibility
- [x] T033 Remove stale `.md` files from root (`MIGRATION.md`, `OLLAMA_SETUP.md`, `CHECKLIST.md`, `API.md`, etc.)
- [x] T034 Migrate LLM provider from Ollama (local) to Groq API (cloud) — update `requirements.txt`, `config.py`, create `groq_service.py`, delete `ollama_service.py`, update `routes.py`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — started immediately
- **Foundational (Phase 2)**: Depended on Phase 1 — blocked all user stories
- **US1 (Phase 3)**: Depended on Phase 2 — P1, implemented first
- **US2 (Phase 4)**: Depended on Phase 2 — P2, implemented after US1
- **US3 (Phase 5)**: Depended on Phase 2 + partial US1 (routes) — P3
- **Infrastructure (Phase 6)**: Depended on all stories being complete
- **Polish (Phase 7)**: Final phase, no blockers

### Within Each User Story

- Tests written and verified to fail BEFORE implementation
- Models/schemas before services
- Services before routes/endpoints
- Backend routes before frontend templates

---

## Notes

- [P] tasks = different files, no shared state, safe to parallelize
- All tasks marked [x] are COMPLETED in the current repository state
- Groq API calls use `response_format={"type": "json_object"}` to enforce structured output
- `uuid4()` file renaming prevents path traversal and encoding issues with non-ASCII filenames
- Commit after each phase checkpoint for clean git history
